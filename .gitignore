# regenerable / throwaway — never in git
node_modules/
*.webm
*.mp4
**/_build/capture/
**/_build/final_frames/
**/_build/shots/
**/_build/frames/
*.log
*.bak
*.tmp

# Office lock/owner temp files (e.g. Word "~$document.docx")
~$*

# secrets - never commit
.env
**/.env
